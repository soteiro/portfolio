---
import BaseLayout from "../layouts/BaseLayout.astro";
import HorizontalCard from "../components/HorizontalCard.astro";
import { getCollection } from "astro:content";

const projects = await getCollection("projects");
---

<BaseLayout sideBarActiveItemID="home">
  <section class="pb-12 mt-5">
    <div class="text-xl py-1">Hola 👋</div>
    <div class="text-5xl font-bold">Soy Diego Ignacio</div>
    <div class="text-3xl py-3 font-bold">Desarrollador Full Stack</div>
    <div class="py-2">
      <text class="text-lg">
        Soy un desarrollador web con experiencia en tecnologías como JavaScript, Python, Node.js, React, y bases de datos PostgreSQL. Me apasiona crear soluciones innovadoras y eficientes que impulsen el éxito de los proyectos en los que trabajo.
      </text>
    </div>
  </section>

  <section>
  <div>
    <div class="text-3xl w-full font-bold mb-2">My last projects {"</>"}</div>
  </div>

  {
    projects.map((project) => (
      <HorizontalCard 
        title={project.data.title} 
        description={project.data.description} 
        image={project.data.heroImage} 
        href={`/projects/${project.slug}`} 
        tags={project.data.tags}
      />
    ))
  }
  <div class="divider my-0"></div>

  </section>
  
</BaseLayout>
